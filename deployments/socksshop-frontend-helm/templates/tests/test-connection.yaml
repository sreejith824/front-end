apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "socksshop-frontend-helm.fullname" . }}-test-connection"
  labels:
    {{- include "socksshop-frontend-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "socksshop-frontend-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
